<%- include('../partials/header') %>
<table id="list">
  <thead>
    <tr>
      <th></th>
    </tr>
  </thead>
  <tbody>
    <% flights.forEach(function(i) { %>
      <tr>
        <td><%= i.airline %></td>

        <td><a href="/flights/<%= i._id %>">DETAILS</a></td>
      </tr>
      <% }); %>
    </tbody>
  </table>
  
<%- include('../partials/footer') %>
